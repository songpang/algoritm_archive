level_2_1 : 다음 큰 숫자 ( 2진수 )
# 2진수 변환 함수 -> 복습 해보기

완주하지 못한 선수
# sort를 통한 속도 향상 
# hash함수 사용

체육복
# Greedy 사용
# _lost = [x for x in lost if x not in reserve] 식의 제외처리
# lost = list(set(lost)-set(emptylist)) 식의 중복 제거.

모의고사
## enumerate 를 이용한 index 이용
## 숏코딩을 위한 코드 정리

타겟넘버
## 트리 구조 완전 간단 코딩
## DFS 개념 파악하기.
## 1. 재귀 2. 단순 2중 for문

같은 숫자는 싫어
## answer[-1:0] 하면 빈 배열에도 오류가 없으며 
## 리턴값은 [마지막 원소값] -> []안에 있음.
## answer[-1] 이랑은 다름.

나누어떨어지는숫자배열
#  return answer or else [-1]로 더 단순화 할 수 있음.
#  사실 answer를 바로 return으로 돌리면 한 줄 코딩 가능.